server:
  port: 3000
spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/test
    driver-class-name: org.h2.Driver
    username: sa
    password: a
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create